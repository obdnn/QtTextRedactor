# Додаток для нотаток

Простий і зручний додаток для створення та редагування текстових нотаток, побудований на PyQt6 і QML.

## Особливості

- 📝 Створення нових нотаток
- 📁 Відкриття існуючих текстових файлів
- 💾 Збереження нотаток у форматі .txt
- ✏️ Редагування назви та вмісту нотаток
- 🗑️ Видалення нотаток з підтвердженням
- 🎨 Сучасний темний інтерфейс
- 🔍 Автоматичне відстеження змін

## Вимоги

- Python 3.8 або вище
- PyQt6

## Встановлення

1. Клонуйте репозиторій або завантажте файли проєкту

2. Встановіть залежності:
```bash
pip install -r requirements.txt
```

## Структура проєкту

```
TextRedactor/
├── main.py              # Головний файл запуску додатку
├── backend.py           # Бекенд логіка (Python)
├── main.qml             # Інтерфейс користувача (QML)
├── requirements.txt     # Залежності проєкту
├── README.md           # Документація
└── public/
    └── Logo_Ob.png     # Іконка додатку
```

## Запуск

Запустіть додаток командою:
```bash
python main.py
```

## Використання

### Створення нової нотатки
Натисніть кнопку **➕ Create** для створення нової порожньої нотатки.

### Відкриття файлу
Натисніть кнопку **📁 Open** і оберіть текстовий файл (.txt) для відкриття.

### Збереження нотатки
- Натисніть **💾 Save** для збереження поточної нотатки
- Якщо файл вже був збережений, зміни збережуться автоматично в той же файл
- Якщо це нова нотатка, відкриється діалог вибору місця збереження

### Видалення нотатки
Натисніть **🗑️ Delete** для видалення поточного файлу (кнопка видима тільки для збережених файлів). З'явиться діалог підтвердження.

### Редагування
- Верхнє поле — назва нотатки
- Нижнє поле — вміст нотатки
- Всі зміни автоматично синхронізуються

## Технології

- **PyQt6** — фреймворк для створення графічних інтерфейсів
- **QML** — декларативна мова для створення користувацьких інтерфейсів
- **Python** — мова програмування для бекенд логіки

